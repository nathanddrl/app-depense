// design-system/pwa — installation PWA (CP1). Le bouton s'auto-masque tant
// que le navigateur ne signale pas l'app installable (`beforeinstallprompt`)
// ou si elle est déjà installée (`display-mode: standalone`).

export { InstallAppButton } from "./install-app-button";
