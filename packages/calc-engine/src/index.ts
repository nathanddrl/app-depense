// @app/calc-engine — placeholder de squelette (aucune logique métier).
// Seule source de vérité du moteur de calcul (archi ch.1.4 / DA4) : pipeline
// canonique, contribution(m), arrondi (D5), plafond aide (D11) à venir.
// Contrainte : fonctions pures, zéro I/O, aucune dépendance domain-*/db.
export const CALC_ENGINE_READY = true;
