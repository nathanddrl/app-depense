// Config Vitest de @app/domain-settlement : réexporte la base partagée (archi ch.1.5).
// Machine à états pure — aucun mock/port requis pour ces tests (DA11).
export { default } from "../config/vitest.base";
