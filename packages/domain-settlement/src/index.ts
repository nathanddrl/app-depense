// @app/domain-settlement — placeholder de squelette (aucune logique métier).
// Orchestration régularisation double approbation, machine à états
// pending/confirmed/cancelled (spec ch.5.3). Importe calc-engine + shared.
export const DOMAIN_SETTLEMENT_READY = true;
