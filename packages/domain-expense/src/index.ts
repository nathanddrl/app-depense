// @app/domain-expense — placeholder de squelette (aucune logique métier).
// Orchestration dépenses ponctuelles, ratio, dissociation payeur/répartiteur
// (spec ch.5.1). Importe calc-engine + shared, jamais réimplémenté.
export const DOMAIN_EXPENSE_READY = true;
