// @app/domain-recurrence — placeholder de squelette (aucune logique métier).
// Orchestration templates/occurrences, appelé par le route handler cron
// (spec ch.5.4). Importe calc-engine + shared.
export const DOMAIN_RECURRENCE_READY = true;
